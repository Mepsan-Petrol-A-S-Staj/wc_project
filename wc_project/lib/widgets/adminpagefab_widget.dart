import 'package:flutter/material.dart';

class AdminPageFABWidget extends StatelessWidget {
  const AdminPageFABWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: const Icon(Icons.add),
    );
  }
}
