import 'package:flutter/material.dart';

class AdminDeviceWidget extends StatelessWidget {
  const AdminDeviceWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Admin Device Page");
  }
}
