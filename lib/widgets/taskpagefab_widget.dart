import 'package:flutter/material.dart';

class TaskPageFABWidget extends StatelessWidget {
  const TaskPageFABWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: const Icon(Icons.done),
    );
  }
}
